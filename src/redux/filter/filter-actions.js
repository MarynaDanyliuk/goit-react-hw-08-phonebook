import { createAction } from '@reduxjs/toolkit';

export const setFilter = createAction('filter/set');

// ____________redux__________________________
// import { createAction } from '@reduxjs/toolkit';

// export const setFilter = createAction('filter/set');
